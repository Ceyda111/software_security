
""" Part 1: Introduction and Authentication (Student 1)
Konu: Sisteme giriş ve şifre güvenliği.

"Hello. Our project is a Secure Vault app. It has three main security steps: Login, Roles, and Encryption. I will talk about Login.

In our code, we don't save passwords as normal text. This is very dangerous because hackers can read them. We use a technique called Hashing.

When you create a password, the system changes it into a secret code. We also add a Salt to make it stronger. Only the correct password can enter the app. This keeps our users safe. Thank you."

Part 2: Access Control - RBAC (Student 2)Konu: Kim nereye girebilir? (Yetki Kontrolü).
"Hi everyone. After login, we have Access Control. This means not every user can see every page.  We used RBAC, which means Role-Based Access Control. In our app, there are two people: Admin and User.  A normal User can only add a note. But they cannot see the Admin page. We have a special check in our code: if session.role is not admin, access is blocked. This protects private information from the wrong people. Thanks."

Part 3: Data Privacy and Encryption (Student 3)Konu: Verilerin şifrelenmesi.
"I will explain Encryption. This is the most important part for data privacy.  Sometimes hackers steal the database. To stop them, we use the AES algorithm. When a user saves a note, our app encrypts it.  Inside the database, the note looks like 'random garbage'. You cannot read it without a Key. Only the Admin can use the key to see the real message. This is called Decryption. Our project follows all security rules to keep data safe. Thank you for listening."
 """